@AGENTS.md

# ドメイン概念：スポットとプレイスリスト

## スポット（Spot）
単一の地理的地点を表すエンティティ。緯度・経度を持ち、地図上に1つのピンとして表示される。

- ユーザーが `/spots/create` から新規作成・登録できる
- マイページの「登録したスポット」（`/mypage/spots`）で管理する
- 詳細ページ（`/spots/[id]`）ではその1地点の情報・説明を表示する
- スポット単体にもいいねができる

## プレイスリスト（PlaceList）
複数のスポットをまとめたコレクション。音楽のプレイリストに相当するイメージ。

- ユーザーが `/placelists/create` から既存スポットを選択して作成する
- 詳細ページ（`/placelists/[id]`）では含まれるスポットを一覧・地図（複数ピン）で表示する
- ホーム画面のカードや各種リストで主役となる表示単位

## 操作の対象と意味

| 操作 | 対象 | 説明 |
|------|------|------|
| **登録** | プレイスリスト | 自分でプレイスリストを新規作成すること |
| **保存** | プレイスリスト | 他のユーザーのプレイスリストをブックマークすること |
| **いいね** | プレイスリスト（スポットも可） | 評価・お気に入りのマーク |

## マイライブラリの構造

マイライブラリ（`/mylibrary`）はプレイスリストに対する操作を3種に分けて管理する。

```
マイライブラリ
├─ 登録したプレイスリスト（自分が作ったもの）
├─ 保存したプレイスリスト（他者のものをブックマーク）
└─ いいねしたプレイスリスト（いいねしたもの）
```

## 階層関係

```
PlaceList（コレクション）
  └── Spot[]（地点の配列）
```

スポットはプレイスリストに属する子要素だが、スポット単体でも独立して存在・管理できる。
